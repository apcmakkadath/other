Register-ScheduledJob -Name "MyJob" `
    -FilePath "C:\test\script.ps1" `
    -Trigger (New-JobTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Minutes 10) -RepetitionDuration ([TimeSpan]::MaxValue))
